import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { CadTelPage } from './cad-tel';

@NgModule({
  declarations: [
    CadTelPage,
  ],
  imports: [
    IonicPageModule.forChild(CadTelPage),
  ],
})
export class CadTelPageModule {}
